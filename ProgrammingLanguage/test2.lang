import print
golden_ratio = function(p)
    fib = function(f, n)
        if n <= 1 then
            return n
        end
        return f(f, n - 1) + f(f, n - 2)
    end
    return fib(fib, p - 1) / fib(fib, p)
end

print(golden_ratio(20))